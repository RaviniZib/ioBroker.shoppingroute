# Security policy

Please report security-relevant problems through a private GitHub security advisory once the repository is public.

This adapter operates only on local ioBroker states. It does not expose its own web server and does not require internet-facing ports.
